# Vila Falo Environment Variables Configuration
# Copy this to .env and fill in your actual values

# Application Settings
NODE_ENV=production
PORT=5000

# MongoDB Database Connection
MONGODB_URI=your_mongodb_connection_string_here

# Email Configuration (REQUIRED for booking confirmations)
# Use Gmail SMTP settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false

# Gmail Email Account
EMAIL_USER=vilafalo@gmail.com

# Gmail App Password (REQUIRED - see setup instructions below)
EMAIL_PASS=your_gmail_app_password_here
GMAIL_APP_PASSWORD=your_gmail_app_password_here

# Gemini AI API Key (Optional - for enhanced chatbot functionality)
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Secret for Authentication
JWT_SECRET=your_secure_jwt_secret_here

# ================================
# EMAIL SETUP INSTRUCTIONS
# ================================
# 
# To get Gmail App Password:
# 1. Go to https://myaccount.google.com/
# 2. Click "Security" in the left sidebar
# 3. Enable "2-Step Verification" if not already enabled
# 4. After 2FA is enabled, look for "App passwords" option
# 5. Click "App passwords"
# 6. Select app: "Mail" and device: "Other (custom name)"
# 7. Enter "Vila Falo Website" as the name
# 8. Google will generate a 16-character password
# 9. Copy that password and use it as EMAIL_PASS above
# 
# IMPORTANT: 
# - Do NOT use your regular Gmail password
# - Use the generated App Password instead
# - Keep this password secure and private
#
# ================================
# GEMINI AI SETUP (OPTIONAL)
# ================================
#
# To get Gemini API Key for enhanced chatbot:
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Create a new API key
# 3. Copy the key and use it as GEMINI_API_KEY above
# 4. The key should start with "AIza"
#
# ================================
# MONGODB SETUP
# ================================
#
# For MongoDB Atlas (recommended):
# 1. Go to https://cloud.mongodb.com/
# 2. Create a free cluster
# 3. Create a database user
# 4. Get the connection string
# 5. Replace <password> with your actual password
# 6. Use that string as MONGODB_URI above
#
# Example MongoDB URI format:
# mongodb+srv://username:password@cluster.mongodb.net/vilafalo?retryWrites=true&w=majority
